# GRM Fire v1 — пожары и пожаротушение

**Дата:** 2026-08-13 · **Статус:** предложение к обсуждению, код не изменён  
**Модуль при реализации:** Код 58  
**Не трогает:** FFD, Q-меню (v4.1.1 закрыто), двери v3, розыск, глобалы `OpenAdminMenu` / `OpenLeaderMenu` / `refreshAllUI` / `Factions`.

---

## 1. Зачем и чего нет

Готовой системы пожаров в репо нет. Ванильный `Entity:Ignite` / тул `igniter` — не база: хаос, нет стадий, нет тушения с прогрессом, нет прав, не переживает рестарт, игрокам `igniter` уже в `toolDeny` Q-меню (категория danger).

Есть только стыки, которые должны стать **источниками**, а не жить отдельно:

| Стык | Сейчас | Должно стать |
|---|---|---|
| Плита `grm_food_stove` | звук `gascan_ignite1`, пожара нет | шанс очага при готовке / без присмотра |
| Принтер `grm_money_printer` | тепло → `Break("перегрев")`; взрыв `env_explosion` 70/180 | перегрев → поломка **и** очаг; взрыв поджигает соседей |
| Пресс `grm_money_press` | тепло → стоп, чинится через терминал | перегрев → стоп + шанс очага (пожар в банке) |
| Сигналка Код 63 | датчик **движения**, kind=`breach` | новый kind=`fire`, дымовой датчик |
| `/grm_alarm_notify` | фракции на взлом | **отдельный** список на пожар (полиция ≠ пожарные) |
| Мини-карта | `AddTempPoint` на взлом | маркер «ПОЖАР» адресатам |
| Фракции | общая таблица `Factions`, пожарных в коде нет | имя не хардкодить, назначает админ |

Цель v1: очаг как сущность сервера, понятные источники, тушение с прогрессом, фракция пожарных через AccessManager, оповещение, без лагов и без ваншота.

---

## 2. Модель: инцидент → ячейки, не ванильный Ignite

Сервер — единственная истина. Клиент рисует пламя/дым/HUD.

**Инцидент** (`incident`) — один пожар как событие (адрес, причина, кто виноват, статус).  
**Ячейка** (`cell`) — кусок пространства в сетке (рекомендую шаг **64 юнита**). В инциденте много ячеек.

Почему не «поджечь проп и забыть»:

- проп унесли / удалили — огонь исчезает или зависает;
- `FindInSphere` по всей карте каждый тик — смерть тикрейта;
- рестарт стирает ванильный огонь;
- нельзя отличить тление плиты от адского пожара банка.

Ванильный `Ignite()` — только косметика на горючем пропе, если ячейка жива. Сняли ячейку — гасим и эффект.

Лимиты (конфиг, не магия):

- макс. инцидентов на карту: **8**;
- макс. ячеек на инцидент: **48**;
- тик симуляции: **0.5 с**, только активные инциденты;
- распространение — по **соседям сетки** (6 сторон), не сфера по карте;
- кулдаун шага распространения: 2–4 с от стадии.

Идентичность ячейки: ключ `map + floor(x/64) + floor(y/64) + floor(z/64)`. Два очага в одной клетке сливаются, интенсивность берётся большая.

---

## 3. Стадии ячейки

Интенсивность `0…100`. Стадия считается с неё, не отдельным enum «для красоты».

| Стадия | Интенс. | Видно | Урон игроку (радиус ~96) | Спред | Чем гасить |
|---|---|---|---|---|---|
| Тление | 1–24 | дым, искры | нет / 1 HP / 2 с | нет | затоптать, вода из инвентаря, огнетушитель |
| Горение | 25–59 | пламя, свет | 3–5 HP / с | медленно, горючие соседи | огнетушитель |
| Сильный | 60–89 | столб, жара | 8–12 HP / с + замедление | быстро | шланг от гидранта; огнетушитель почти бесполезен |
| Выгорание | 90–100 → спад | чёрный дым | как «горение», падает | стоп | добить огнетушителем / шлангом |
| Потушен | 0 | мокрый след ~15 с | нет | нет | — |

Рост без тушения: тление → горение за 15–25 с, горение → сильный за 30–45 с. Топливо ячейки конечно: выгорела — спад, не вечный ад.

**Не ваншот.** Потолок урона с нескольких ячеек — одна «сильная» порция, не сумма восьми. В воде карты (`MASK_WATER`) ячейка гаснет быстрее, игрок не горит.

Дым: клиентский оверлей + кашель (без урона) в радиусе больше пламени. Медицинский модуль не ломаем: обычный `TakeDamage` / `DMG_BURN`, без своих статусов, пока владелец не скажет иначе.

Пропы: горючие теряют HP и чернеют / удаляются **только если это не карта и не перм-энтити**. Карту и `/permadd` не жжём. Газовый баллон / канистра из сид-листа Q — катализатор (скачок интенсивности соседям один раз), не ядерный взрыв.

---

## 4. Источники огня

Каждый старт пишет в инцидент: `source`, `pos`, `starter` (`CharacterKey` или `system`), `started`.

### 4.1. Плита (`grm_food_stove`) — быт

Сейчас готовка: списали ингредиенты → `StoveState=1` → таймер → блюдо на лоток, плита гаснет. Звук огня есть, риска нет.

Правило v1 (числа в конфиге):

1. Пока идёт готовка (`state==1`) — раз в N секунд бросок.
2. Рядом есть игрок (≤ 200 юн) — шанс низкий (**~0.4 % / с**, «присматривает»).
3. Никого нет — шанс выше (**~1.2 % / с**, бросили сковороду).
4. Длинный рецепт (суп 90 с) опаснее короткого (коктейль 45 с) — множитель от `rec.time`.
5. Старт очага — **тление на клетке плиты**, не взрыв. Готовка срывается, ингредиенты сгорают (не возвращаем). Лоток готового портится в `grm_food_spoiled`, если уже лежал.

Плита сама по себе после готовки огонь не держит: она выключается. Риск — только пока жарит.

### 4.2. Денежный принтер — криминал

Сейчас: `heat >= 100` → `Break("перегрев")`, тихий отказ. Взрыв только с HP=0.

Правило v1:

- перегрев → как сейчас `Break` **плюс** тление/горение на клетке принтера;
- `DestroyPrinter` (взрыв 70/180) → поджечь горючих соседей в радиусе взрыва, не всю комнату;
- починить принтер можно только когда клетка потушена (иначе чинить горящее — бред).

Ферма принтеров в чулане начинает иметь цену.

### 4.3. Банковский пресс — событие

Сейчас: `heat >= OverheatAt` → стоп, «охладите через терминал».

Правило v1: стоп как сейчас + **шанс** очага (не 100 %: банк не должен гореть каждый цикл). Рекомендую **15–25 %** на факт перегрева. Охлаждение через терминал не тушит уже занявшийся очаг.

### 4.4. Поджог

Игрокам ванильный `igniter` **не открываем** (Q-меню не трогаем).

| Кто | Чем | Следствие |
|---|---|---|
| SuperAdmin | команда / тул «очаг в прицеле» | `starter=admin`, без статьи |
| Преступник (опционально этап 7) | предмет «канистра / факел», не тулган | `starter=CharacterKey`, хук на розыск (статья — когда скажете) |

v1 без криминального поджога — нормально. Админский спавн обязателен для тестов и ивентов.

### 4.5. Не в v1 (задел)

Молния, проводка карты, костёр/бочка, авто после аварии, взрыв гранаты. Хуки наружу будут: любой модуль вызовет `GRM.Fire.Ignite(pos, source, starter)`.

---

## 5. Чем тушить и как

Тушение — **не клик = погас**. Каждый инструмент снимает интенсивность с ячейки под прицелом / конусом. Сильный пожар одним баллоном не берётся.

### 5.1. Гражданские средства (любой игрок)

| Средство | Что делает | Лимит | Где взять |
|---|---|---|---|
| Затоптать (E / присед на тлении) | −8…−15 с ячейки тления | ожог, если уже «горение» | всегда |
| Бутылка воды `grm_food_water` | −10 с одной клетки тления/слабого горения, предмет тратится | против сильного — ничто | автомат / инвентарь |
| Огнетушитель SWEP `weapon_grm_extinguisher` | конус ~180 юн, −X/с, ЛКМ держать | заряд 100, пустой = шипит | шкаф части, магазин, выдача фракции |

Огнетушитель — основной бытовой ответ. На стадии «сильный» коэффициент ×0.15: баллон кончится, столб останется. Это и есть повод звать пожарных.

Заряд: дозаправка на `grm_fire_cabinet` / станции (E, 3–5 с). В v1 без экономики на заправку, если не скажете иначе.

### 5.2. Проф. средства (слой FightPro)

| Средство | Условие | Что делает |
|---|---|---|
| Гидрант `grm_fire_hydrant` | SuperAdmin ставит, `/permadd` | точка воды на карте, заморозка физики |
| Шланг SWEP `weapon_grm_hose` | игрок ≤ 250 юн от **открытого** гидранта (или позже — машины) | сильная струя, −Y/с даже по «сильному», дальность ~400 |
| Шкаф / станция `grm_fire_cabinet` | perm | взять/вернуть огнетушитель, заправить, (опц.) надеть шланг |

Гидрант: E открыть/закрыть. Открытый без шланга просто «течёт» (звук, лужа-декор), воду карты не заливает. Шланг без гидранта — пустой рукав.

Машину / автоцистерну в v1 не делаем. Слот под неё: шланг сможет принимать «источник воды» не только гидрант.

### 5.3. Что не тушит

- Обычный пистолет, дубинка, таран, мегафон.
- «Выключить плиту» после старта очага очаг не гасит (готовка уже сорвана).
- Охлаждение пресса через терминал — только тепло станка, не пламя.
- Remover / physgun очаг не снимают. SuperAdmin гасит командой.

### 5.4. Прогресс и «кто потушил»

Инцидент `active`, пока есть ячейка > 0. Когда все нули — `extinguished`, пишем время и список `CharacterKey`, кто наносил урон огню (для отчёта / ачивок). Маркер мини-карты снимается (`RemoveTempPoint`).

Ложный вызов (дернули извещатель, очага нет) — отдельный статус `false_alarm`. Штраф — вопрос владельцу, в v1 можно только писать в журнал.

---

## 6. Доступы (четыре контура, не путать)

Как двери/сигналка/розыск: **свой** AccessManager, файл `data/grm_fire/access.json`.  
`AM.CanManage` ≠ SuperAdmin. SuperAdmin обходит всё. Название фракции в коде не ищем (`feuer` / `пожар` / `fire` — запрещено).

| Контур | Кто | Что даёт | Чего не даёт |
|---|---|---|---|
| 0 SuperAdmin | `IsSuperAdmin` | спавн/гашение, гидранты на карте, `/fire_access`, админка | — |
| 1 Гражданское тушение | **все** (дефолт) | затоптать, вода, огнетушитель | шланг, гидрант, пульт, админку |
| 2 FightPro | фракции/роли/отделы/Steam из access | шланг, открыть гидрант, шкаф проф. снаряжения | править матрицу, спавнить огонь |
| 3 Dispatch | те же списки, поле View | видеть пульт/журнал, маркер карты, закрыть карточку | тушить шлангом само по себе |
| 4 Manage | SuperAdmin (+ опц. CanManage только открыть меню) | `/fire_access` | вкладки дверей/розыска |

Сохранить матрицу — **только SuperAdmin** (как `/door_access` после v3).  
Steam в JSON — **массивы CharacterKey**, не карта числовых ключей. Чтение `jsonT(..., false, true)`. `version=1`. Битый файл → карантин `.corrupt.<ts>`, не затирать. Read-back после записи.

Открыть меню: `/fire_access`, `!fire_access`, вкладка «Пожарные» в `/factions` через существующий хук `OpenAdminMenu` (**глобал не локализуем**).

Рекомендуемый дефолт с пустым `access.json`: тушить баллоном может каждый; шланг/гидрант/пульт — никто, пока админ не отметит фракцию. Сервер без пожарных всё равно живёт: соседи тушат кухню, банк без FightPro горит до выгорания или админа.

---

## 7. Фракции и оповещение

Отдельной пожарной фракции в коде нет и **не будет**. Админ заводит «Feuerwehr» / «Пожарная охрана» как обычную фракцию и ставит галочки в `/fire_access` + список оповещения.

Оповещение **не делить** с `/grm_alarm_notify` (там взлом). Иначе менты едут на кастрюлю, а пожарные — на кейпад.

Свой список: `data/grm_fire/notify.json` (массив имён фракций), команда `/grm_fire_notify` — клон UX сигналки, только SuperAdmin.

Цепочка при старте инцидента (не при каждом тике):

1. Хук `GRM_FireStarted(incident)`.
2. Если в радиусе есть дымовой датчик **или** кто-то дернул извещатель **или** стадия уже «горение» — `GRM.Alarm.Log(netID, "fire", text)` (журнал сигналки, kind новый).
3. `Notify` всем онлайн-членам фракций из fire-notify.
4. `GRM.Minimap.AddTempPoint("ПОЖАР", pos, ttl)` + `SendTo` только адресатам и SuperAdmin, не всему серверу.
5. При гашении — снять маркер, хук `GRM_FireExtinguished`.

Дымовой датчик `grm_fire_detector`: perm-энтити, радиус как у датчика движения (220, кламп 64–800), сеть сигналки (`NetworkID`). В режиме «Выкл» не орёт. Это не замена датчику движения и не новый режим хаба.

Уличный извещатель `grm_fire_pull` (опц. этап 6): любой может дернуть. Есть очаг в радиусе — ускоряем оповещение. Нет — `false_alarm`.

Пульт `grm_fire_terminal` (этап 4–5): список активных инцидентов (стадия, клеток, возраст, источник), без выдачи чужих Steam-списков. Открыть — Dispatch или SuperAdmin. Дистанция + rate-limit на нет-сообщениях.

---

## 8. Сеть, UI, персист

**Сеть.** Клиенту — только ячейки в радиусе ~1500 и короткая карточка ближайшего инцидента (стадия, надо ли звать проф.). Полную таблицу инцидентов, ACL и Steam не шлём. Приём действий: дистанция, rate-limit (шланг/огнетушитель — тик оружия, не нет-флуд), повторная проверка слоя. `GRM.UI.Track` — одно окно пульта.

**UI игрока.** Полоска жара, когда в зоне. На SWEP — заряд и «есть ли напор». Тост один раз: «Пожар! Вызовите пожарных» при входе в «сильный», не каждую секунду.

**Персист карты (обязательно):** гидранты, шкафы, датчики, пульт, извещатели — через существующий `/permadd` (Код 50) + `GRM.PermData.Extract/Apply`. Без своего параллельного сейва точек, если хватает перма.

**Персист живых очагов (вопрос владельцу).** Рекомендация: **да, короткий**. Массив в `data/grm_fire/active_<map>.json`: id, клетки (ключ сетки + интенсивность + топливо), source, starter, started. `version=1`, массивы, карантин, read-back. После рестарта пожар не «магически» гаснет — иначе ивент банка обнуляется крашем. TTL: если инциденту > 30 мин реального времени — на загрузке тушить (защита от вечного пожара в пустой клетке).

Права и notify — отдельные файлы, не смешивать с клетками.

---

## 9. Контракт наружу (чтобы другие модули не лезли внутрь)

```
GRM.Fire.Ignite(pos|ent, source, starterKey) → incidentId|nil
GRM.Fire.Extinguish(pos|ent, amount, ply) → bool
GRM.Fire.IsBurning(pos|ent) → bool
GRM.Fire.IntensityAt(pos) → number
GRM.Fire.CanFight(ply)        -- гражданин / огнетушитель
GRM.Fire.CanFightPro(ply)     -- шланг, гидрант
GRM.Fire.CanDispatch(ply)     -- пульт, маркеры
GRM.Fire.CanManage(ply)       -- открыть /fire_access
GRM.Fire.Config / GRM.Fire.Data
```

Хуки: `GRM_FireStarted`, `GRM_FireSpread`, `GRM_FireExtinguished`, `GRM_FireFalseAlarm`.

Сигналка, кухня, принтер, пресс зовут только `Ignite` / хуки. Q-меню, FFD, двери не знают про пожар.

---

## 10. Чего модуль не делает

- Не открывает игрокам `igniter` / dynamite / turret.
- Не переписывает плиту, принтер, пресс целиком — только точка вызова `Ignite` и «не чинить горящее».
- Не создаёт фракцию «пожарные» в `Factions`.
- Не ставит статьи УК и не штрафует сам (розыск — другой модуль; дадим хук).
- Не жжёт map-энтити и пермы.
- Не делает пожарную машину, лестницу, спасение с крыши, страховые выплаты — задел после v1.

---

## 11. Этапы внедрения (после «Реализовывай»)

| Этап | Содержание | Риск |
|---|---|---|
| 1 | Ядро: инцидент/сетка/стадии, админ `Ignite`/`Kill`, лимиты, эффекты | низкий |
| 2 | Огнетушитель + затоптать + вода из инвентаря | низкий |
| 3 | Стыки: плита, принтер (перегрев+взрыв), пресс | средний |
| 4 | AccessManager + `/fire_access` + notify + маркер карты | средний |
| 5 | Гидрант + шланг + шкаф (perm) | средний |
| 6 | Дымовой датчик, пульт, персист активных очагов | средний |
| 7 | Поджог-предмет, урон пропам, ложный вызов → штраф | высокий |

Этапы 1–3 уже дают «кухня загорелась, тушат баллоном». 4–6 делают профессию. 7 — криминал и полировка.

---

## 12. Вопросы владельцу

1. **Кто тушит проф. техникой?** Одна фракция (вы назначите в меню) или несколько (город + база)? Дефолт «баллон всем» ок?
2. **Плита:** шанс только без присмотра или ещё и «кривые руки» при игроке рядом? Срывать готовку и жечь ингредиенты — ок?
3. **Принтер:** всегда огонь на перегреве или шанс? Взрыв принтера поджигает комнату?
4. **Пресс:** пожар в банке редкий ивент (рекомендую 15–25 %) или каждый перегрев?
5. **Поджог игрокам** в v1 или только админ + быт/техника?
6. **Живые очаги через рестарт** — сохранять (рекомендую) или гасить при загрузке?
7. **Ложный вызов** — только журнал или сразу штраф / розыск?
8. **Урон игрокам** — таблица в §3 ок или мягче/жёстче? Нужен ли медмодулю отдельный ожог?
9. **Огнетушитель в магазине** за деньги или только шкаф части / выдача?
10. **Имена команд** `/fire_access` и `/grm_fire_notify` ок, или хотите по-немецки (`/feuerwehr`)?

Код не пишу, пока не будет «Реализовывай» и ответов хотя бы на 1, 5, 6.
