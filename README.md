# GRM — Garry's Mod RP-сборка (DrStrasse)

> Передача дел новой сессии агента: читать **`HANDOVER.md`** (ветка
> `arena/019f6cb8-drstrasse` — актуальный код; master — старый пустой снапшот).

Набор Lua-модулей для Garry's Mod: фракции, экономика, инвентарь, логистика,
завод полного цикла, видеонаблюдение (CCTV), сигнализации, система розыска, двери,
дверные ключи `ds_key_swep`, FFD Fading Door, FFD Keypad с 3D2D экраном, полицейский таран `ds_battering_ram`, QTE-отмычка `ds_lockpick`,
телефонная связь, стамина, RP-чат и персистентность. Все модули общаются через
глобальный namespace `GRM` и глобальную таблицу `Factions`.

## Установка

Скопировать содержимое `lua/` в `garrysmod/addons/grm/lua/`
(или прямо в `garrysmod/lua/`). Файлы в `lua/autorun/` загружаются
автоматически на сервере и клиенте.

**Начиная с Кода 88 (мобильные телефоны)** вместе с `lua/` нужно
скопировать и папки `models/ivancorn/` + `materials/models/ivancorn/`
(модели трубок GTA IV by ivancorn) — иначе мобильные будут ERROR.
Готовый `dist/grm_single_addon.zip`/`grm_full_code.zip` уже содержит
всё тремя папками (lua, models, materials).

## Файлы (основные модули)

| # | Файл | Назначение |
|---|------|-----------|
| 1 | `lua/autorun/sh_grm_logistics_entities.lua` | Регистрация entity логистики: точка погрузки, склад фракции, оружейный шкаф, грузовой ящик |
| 2 | `lua/autorun/server/sv_grm_logistics.lua` | Сервер логистики: рейсы матовозок, ящики (2 пистолета + 5 автоматов), склады, шкафы, сохранение на карту |
| 3 | `lua/autorun/sh_grm_factory_fullcycle_config.lua` | Конфиг завода: рецепты GPU/компонентов/оружия ArcCW, переплавка брака, рынок, QTE |
| 4 | `lua/autorun/sh_grm_factory_fullcycle_entities.lua` | Регистрация entity завода: станки, мусорка, терминал, склад, скупщик, шкаф |
| 5 | `lua/autorun/server/sv_grm_factory_fullcycle.lua` | Сервер завода: крафт, QTE-сессии, лом, продажа GPU, скупщик/шкаф оружия, сохранение |
| 6 | `lua/autorun/sh_grm_inventory.lua` | Инвентарь: 24 слота, стаки, оружие/патроны/предметы, выброс `grm_item_drop` (с `/drop`), JSON-персистентность |
| 7 | `lua/autorun/sh_grm_movement.lua` | Стамина: бег/прыжки, звук дыхания (CreateSound), HUD-полоса выносливости |
| 8 | `lua/autorun/sh_grm_chat_config.lua` | Конфиг чата: радиусы local/whisper/yell/LOOC, цвета, настройки контекстного меню |
| 10 | `lua/autorun/sh_factions.lua` | Ядро фракций: ранги, отделы, приглашения, рация `/fr`, волна `/dep` `/depb`, меню `/factions`, HUD-таблички |
| 11 | `lua/autorun/sh_faction_fixes.lua` | Расширение фракций v3.1.1: комендантский час `/kom_hour`, модели+bodygroups, оружие по рангам, маскировка V2, `/gnews`. Вкладка **«Расширенные настройки» в /factions** (хук `GRM_FactionsAdmin_BuildTabs`): живые счётчики моделей/оружия/маскировки, статус и отмена ком.часа, доступ /gnews — единый стейт с `/model`, спавном оружия, `/mask`, авто-синк 1.5 с; оружие теперь сохраняется на диск |
| 13 | `lua/autorun/sh_grm_admin_menu.lua` | Суперадмин-меню экономики v1.1: балансы, персональные налоги, переводы, журнал действий — `!grmmenu` |
| 14 | `lua/autorun/sh_grm_shop_integration.lua` | Интеграция магазина/дилера: сканер транспорта (GMod/SimFPhys/LVS/Glide), вкладка «Транспорт» в меню лидера, `/scanvehicles` `/vlist` |
| 15 | `lua/autorun/sh_spawn_points.lua` | Точки спавна фракций/глобальные (per-map JSON), админ-меню `/spawnmenu`, случайный спавн по точкам |
| 16 | `lua/autorun/sh_grm_vehicle_access.lua` | Доступ к транспорту: персональные покупки, доступ по фракции/рангу/отделу, магазин `/vshop`, админ цен `/vshop_admin` |
| 17 | `lua/autorun/vehicle_dealer.lua` | Патч дилера v3: проверка доступа перед спавном, фильтр списка, блок Q-меню, кулдаун 2с, лог спавнов |
| 18 | `lua/autorun/zz_grm_vehicle_antistuck.lua` | Анти-застревание при выходе из машины (NoCollide + поиск безопасной точки, simfphys/LVS), `zz_` грузится последним |
| 19–23 | `lua/autorun/sh_grm_phone_*.lua` | Полная система телефонии: конфиги, доступы, магазин, сервер и клиент связи |
| 24 | `lua/autorun/sh_grm_logistics_config.lua` | Конфиг логистики: дистанции, ящики, награды, матовозка `simfphys_gta_sa_barracks` |
| 25–39 | `lua/entities/grm_*` | Сущности таксофона, АТС, стационарного телефона, терминалов и прослушки |
| 40–41 | `lua/autorun/client/cl_grm_*.lua` | Клиентские части логистики и завода полного цикла |
| 42 | `lua/autorun/sh_grm_currency.lua` | **Ядро валюты v2.0.2:** `GiveMoney/TakeMoney/HasMoney/GetBalance/SetBalance/Format/Notify`, JSON-персистентность |
| 43 | `lua/autorun/sh_grm_economy.lua` | **Единая экономика v3.0.3:** бюджеты, налоги, зарплаты, штрафы `/fine`, банк-терминалы, зеркало по никам, склеивание ключей |
| 44–46 | `lua/entities/grm_bank_terminal/` | **Банкомат** (`models/starless/atm.mdl`): личные счета, переводы, управление бюджетом фракций |
| 47 | `lua/autorun/sh_grm_tab_menu.lua` | Tab-меню v1.9 (scoreboard): RP-имя вместо Steam-ника (стим мелкой строкой в карточке), список игроков, фильтры, балансы, глушение голоса |
| 48 | `lua/autorun/client/cl_grm_hud.lua` | HUD v10.2: Здоровье/Броня, Наличные, Банк, Патроны, Селектор оружия, Уведомления |
| 49 | `lua/autorun/client/cl_grm_inventory_ui.lua` | GUI инвентаря v2.1: сетка 6×4, Drag&Drop, детали предметов |
| 50 | `lua/autorun/sh_grm_perm_entities.lua` | **Пермы энтити v1.1.0:** Закрепление банкоматов, телефонов, АТС, CCTV и сигнализаций на карте |
| 51–54 | `lua/autorun/sh_grm_cctv_*.lua` | **CCTV Видеонаблюдение:** конфиг, доступы, сервер, клиент и сущности `grm_cctv_camera`, `grm_cctv_monitor`, `grm_cctv_server` |
| 55–58 | `lua/autorun/sh_grm_alarm_*.lua` | **Система Сигнализаций:** конфиг, доступы, сервер, клиент и сущности `grm_alarm_sensor`, `grm_alarm_hub`, `grm_alarm_terminal` |
| 59–60 | `lua/autorun/sh_grm_wanted_*.lua` | **Система Розыска:** статусы wanted/warrant, причины, серверная обработка, клиентский HUD и списки |
| 61–62 | `lua/autorun/sh_grm_doors*.lua` | **Система Дверей v2.0.5 & Access v2.2.0:** авторитетный реконсилер замков, 3D2D HUD, подавление сторонних оверлеев, меню сохраняет вкладку/прокрутку, кастомные категории фракций (создание/объединение в `/factions` → «Двери и Ордера»), категории в ACL дверей |
| 63–64 | `lua/autorun/sh_grm_rp_chat.lua` | **RP Чат и EasyChat патчи:** команды `/me`, `/do`, `/it`, `/try`, `/roll`, `/w`, `/y`, `/looc`, `/ooc` |
| 65 | `lua/entities/grm_item_drop/` | **Выброшенный предмет/оружие:** сущность 3D-модели предмета при выбросе из инвентаря/рук (`/drop`) |
| 66 | `lua/weapons/ds_key_swep/` | **Дверные ключи `ds_key_swep`:** оружие для блокировки (ЛКМ), разблокировки (ПКМ) и вызова меню двери (R) |
| 67 | `lua/weapons/ds_battering_ram/` | **Полицейский таран `ds_battering_ram`:** вскрытие дверей по ордеру на обыск `/warrant` или праву ForceDoor |
| 68 | `lua/weapons/ds_lockpick/` | **QTE-Отмычка `ds_lockpick`:** интерактивный взлом запертых дверей и кейпадов с подбором пинов |
| 69 | `lua/weapons/gmod_tool/stools/ffd_fading_door.lua` | **FFD Fading Door:** тулган создания исчезающих дверей с нумпадом, инверсией и авто-закрытием |
| 70 | `lua/entities/grm_keypad/` | **FFD Keypad (`grm_keypad` & `ffd_keypad.lua`):** интерактивный кодовый замок с 3D2D экраном, PIN, фракционным доступом и платным проходом |
| 71 | `lua/autorun/sh_grm_rpdesc.lua` | **RPDesc v2.1.0:** RP-имя + описание над головой ВСЕМ (включая себя, 1-е/3-е лицо), редактор `/rpdesc`, конвары grm_cl_rpdesc(+_dist), лимит 420 симв., анти-флуд |
| 72 | `lua/autorun/sh_grm_character.lua` | **Ядро персонажей GRM Identity v1.1.0:** меню при КАЖДОМ входе (ширина ×2), RP-имя (`/name`, NWString GRM_RPName), провайдер-патчи `RegisterProvider` (гражданский+фракционный гардероб), синхрон с фракционным спавном, `/char` |
| 73 | `lua/entities/grm_wardrobe/` + `server/sv_grm_wardrobe_spawn.lua` | **Гардероб:** шкаф `props_interiors/Furniture_CabinetDrawer01a` (фолбэк на локер при отсутствии модели), E → меню внешности (фильтры + особые/скрытые модели, суперадмин E→⚙), `/wardrobe_add` `/wardrobe_remove`, перм-класс grm_wardrobe, конфиг в data/grm_wardrobe/<map>.json (выживает рестарт) |
| 74 | `lua/autorun/sh_grm_f4menu.lua` | **F4-меню v1.4.1:** вкладки Профиль / Команды / Настройки (HUD-выключатели) / **Графика** (пресеты FPS+/Красота, 10 тоглов, слайдеры дальности/качества) + точка расширения `GRM_F4_BuildTabs` (вкладки «Работа»/«Ачивки» от модулей). Шпаргалка команд: +радио/оповещение/доска/доступы/биржа/ачивки/багажник/админ-панель. F4 ловится биндом и прямым опросом (toggle), уступает дверям; `/menu` `/f4` |
| 75 | `lua/autorun/sh_grm_broadcast.lua` + `lua/entities/grm_radio/`, `grm_broadcast_mic/`, `grm_loudspeaker/` + мост `sh_grm_factions_bridge.lua` | **Радиовещание + массовое оповещение v1.1.0:** микрофон (Black Ops p_int_microphone; `/bcast_allow`) — голос+текст в эфире до приёмников; радио citizenradio (E — станция); громкоговорители → `/alert` `/alertall` (команды идут через PlayerSayTransform — не проглатываются чат-системой); доступы и из `/factions` → «Доступы»; спавн `/radiomic_add` `/radio_add` `/speaker_add` — АВТОперсистентность без /permadd (grm_bcents/<map>.json) |
| 76 | `lua/autorun/sh_grm_board.lua` + `lua/entities/grm_board/` | **Доска объявлений/набор во фракции v1.1.0** (модель corkboardverticle01): доступ фракциям: `/factions` → «Доступы», суперадмин E→⚙ у доски, `/board_allow`, лидер открывает/закрывает набор, игрок E→«Вступить» попадает во фракцию автоматически (FactionsAPI), лидеру — сведения (ник, RP-имя, SteamID, время) + журнал 20 записей; **автозачисление (v1.1.0): лидер с доступом у доски задаёт кнопкой «Отдел/должность» КУДА вступающий зачисляется сразу (Cfg.assign, проверка против списков фракции, роль лидера запрещена, сброс в «по умолчанию») — применяется через FactionsAPI.SetMemberRole/SetMemberDepartment, фиксируется в журнале и отчёте лидеру; `/board_add`** |
| 77 | `lua/autorun/sh_grm_jobs.lua` + `lua/entities/grm_jobcenter/`, `grm_depot/` | **Биржа труда v1.1.0:** терминал (модель `models/lt_c/holo_wall_unit.mdl` — голо-панель, фолбэки: консоль → радио, без торговых автоматов; E → вакансии: курьер/патруль/грузчик/инспектор, ротация 5 мин, награда по дистанции, жёсткий дедлайн по настенным часам, 3D-маркер цели, вкладка «Работа» в F4) + точки доставки `/jobdepot_add`; **заказы и ВАКАНСИИ фракций** — лидер с доступом «БИРЖА» (`/factions` → «Доступы», `/job_allow`) публикует разовый заказ (эскроу награды) ИЛИ многоместную вакансию: зарплата за смену × число смен (1–10), длительность 5/10/15/20 мин, зона смены ловится кнопкой «здесь, где стою» (`/jobpost` у станка завода) или терминалом — исполнитель дежурит в зоне 400 юн пешком, после каждой смены выплата из эскроу и счётчик ↓, работодателю — отчёты (вышел/отработал/осталось), в карточке — выплачено и последний исполнитель; отзыв/просрочка — возврат остатка; провал исполнителя вакансию НЕ закрывает (v1.1.0); до 3 публикаций, 24 ч; `/jobs` `/jobcancel`; автоперсистентность (`grm_jobs_ents/<map>.json`, активные задачи массивом в `grm_jobs_active.json` — переживают рестарт); хуки GRM_Jobs_Started/Completed/Failed для ачивок |
| 78 | `lua/autorun/sh_grm_achievements.lua` | **Ачивки и вознаграждения v1.0.1:** 21 достижение (экономика, биржа, радио/оповещение, доска/фракции, часы в городе, пешие километры, стрик входа) с денежными наградами — начисление автоматически при разблокировке (тост «★» + звук + чат), вкладка «Ачивки» в F4 с прогресс-барами, `/ach`; **ежедневный бонус** за вход с растущим стриком (500+250/день, потолок 2000); прогресс из хуков сборки (деньги/задачи/эфир/оповещения/вступление) + тик-поллинг (время, пешком с анти-телепорт-капой); хранение массивом `grm_achievements.json`; `/ach_reset ник` + API `GRM.Ach.AdminReset`/`SaveNow` для админ-хаба |
| 79 | `lua/autorun/sh_grm_admin_hub.lua` | **Единая админ-панель v1.0.0** (`/grm_admin`, консоль `grm_admin`, суперадмин): вкладка «Сервер» (онлайн/карта/аптайм/версии модулей/счётчики сборки/список фракций), «Доступы» (матрица фракция × ДОСКА/ЭФИР/ОПОВЕЩ/БИРЖА — тот же стейт, что `/factions` → «Доступы» и чат-команды, собственный net-протокол без войны за receiver'ы моста), «Биржа» (активные задачи игроков с провалом, публикации фракций с удалением и возвратом остатка эскроу), «Игроки» (баланс/RP-имя/ачивки, копия SteamID, сброс ачивок), «Меню» (ярлыки всех админ-меню сборки одним нажатием) |
| 80 | `lua/autorun/sh_grm_trunk.lua` | **Багажник любого транспорта v1.0.0** (`/trunk`, `/багажник`, консоль `grm_trunk` — глядя на машину): открытие/закрытие крышки (звук ящика, 3D2D-метка «БАГАЖНИК ОТКРЫТ»), общий лут ВСЕЙ техники сборки (стандарт/simfphys/LVS/LFS/дилер, классификация движком VK_isVehicle), доступ зеркалит ключи: владелец и фракция — всегда, чужой — только пока машина разблокирована (RP-кража на совести владельца), суперадмин — всегда; 24 слота/120 кг (вес из инвентаря, оружие 3 кг/шт — оверрайд `GRM.Trunk.ClassCaps`), переносы только на сервере (анти-дюп: клэмп по наличию, общий снапшот зрителям); автозакрытие на ходу (~33 км/ч), при уходе всех или по таймауту 20 с; персист `grm_trunks.json` по ключу владелец+класс (личная и фракционная техника), машины без владельца — только на сессию |
| 81 | `lua/entities/grm_money_drop/` + правки `sh_grm_currency.lua` (v2.0.3), `sh_grm_inventory.lua` | **Деньги как физический предмет v1.0.0:** `/dropmoney <сумма>` — пачка наличных на землю (эентити `grm_money_drop`, модель по заказу `models/props/cs_assault/money.mdl`, фолбэк коробка; E → в кошелёк, живёт 10 мин, хук GRM_Money_Picked); `/money_pack <сумма>` (= /cashout) — наличные в инвентарь предметом «Деньги» (число в стаке = сумма, вес 0.001/ед, maxStack 50 000, модель та же — и для дропа из инвентаря `grm_item_drop`, и для хранения в багажнике/складе); «Использовать» предмет — обналичить весь стак обратно (useFunc cash_to_wallet, хук GRM_Money_Cashed); отказ при нехватке средств, возврат не влезшего при полном инвентаре; чат-контракт находки 89; сим-стенд `tools/luatest/sim_money.lua` |
| 82 | `lua/entities/sent_vehicle_dealer/` (init/cl_init) + `lua/autorun/sh_grm_ctx.lua` + `sh_grm_admin_hub.lua` v1.1.0 | **Дилер 2.1 + контекст транспорта:** дилер ПРОДАЁТ — цена из меню списывается с наличных (служебный фракции бесплатно, админ-спавн из ТАБ — тоже), при фейле спавна мгновенный возврат; машина сразу оформлена через ключи (закрыта, ключ в кармане), VD_Price запоминается; удаление своего Т/С возвращает **50%**. **UI 2.1:** окно 940×620 с вкладками — **«Каталог»** (поиск, живое 3D-превью модели, баланс наличных, цена, кнопка КУПИТЬ блокируется при нехватке, двойной клик = купить сразу, после покупки авто-переход в гараж) и **«Мой транспорт»** (статусы ЗАКРЫТ/ОТКРЫТ, дистанция до Т/С, сумма возврата на строке, сдача УДАЛЁННО пока стоишь у дилера, «УБРАТЬ ВСЁ» с суммарным возвратом, Derma-подтверждения, автообновление списка каждые 5 с через `VD_MyListReq`). **Единая точка удаления** `VD_RemoveDealerVehicle(ply, veh, opts)` — одинаковая логика (права/возврат/чистка реестра/синк списков/хук) для меню дилера и C-меню: в C-меню кнопка «Убрать Т/С» показывает сумму возврата в подписи и работает с **двойным нажатием-подтверждением**; `/vd_remove` — всё из чата. Вкладка хаба **«Экономика»** (гос.бюджет, бюджеты фракций, наличные — E.StateBudget*/FactionInfo, экономика 3.0.4) |
| 83 | `lua/autorun/sh_grm_qmenu.lua` + вкладка в hub | **Q-меню и инструменты v1.1.0** (вкладка «Инструменты» в `/grm_admin`): переключатели Q-меню/пропов/рагдоллов/эффектов/NPC/SENT/SWEP/транспорта из Q для игроков (суперадмин вне всех ограничений), чёрный список toolgun-инструментов из каталога 34 шт или строгий БЕЛЫЙ режим; **v1.1.0: закрытие Q действует и на ВАНИЛЬНОЕ, и на КАСТОМНОЕ спавн-меню — универсальный слой PlayerBindPress глушит сам бинд `+menu`, плюс конфиг теперь живо синкается всем клиентам (GRM_QMenu_Sync: на вход и после каждой правки из хаба — раньше клиент жил с дефолтом)**; гейты SpawnMenuOpen/ContextMenuOpen + PlayerSpawn*/CanTool (сервер — против любой менюшки и консоли); API GRM.QMenu.CanUseTool/CanSpawn/CanOpenQ; персист grm_qmenu.json; стенды sim_qmenu + sim_rootboard |
| 84 | `lua/autorun/sh_grm_rootguard.lua` + гейт в `sh_factions.lua` | **Защита владельца (Root Guard) v1.0.0** — «защита от идиота-суперадмина»: удаление фракций (и любые будущие защищённые виды действий через `GRM.Root.RegisterExecutor`) требует подтверждения ВЛАДЕЛЬЦА СЕРВЕРА: сид-корневой SteamID **зашит в коде** (не снимается командами/данными); семантика fail-closed — root → мгновенно, root онлайн → всплывающее окно одобрить/отклонить, root оффлайн → заявка висит (фракции целы), рестарт → очередь обнуляется = консервативный отказ; гейт стоит в центральном NET_ACTION (`deleteFaction`), исполнение одобренного — через `FactionsAPI.DeleteFaction`; доп. корни: `/root_add`/`/root_del` (только root, персист grm_rootguard.json), `/root_list`, `/root_queue`; стенд sim_rootboard |
| 85 | `lua/autorun/sh_grm_radionet.lua` + `lua/entities/grm_server_rack/`, `grm_antenna/`, `grm_radio_station/` + `lua/weapons/weapon_grm_megaphone/` + правки `sh_grm_broadcast.lua` (v1.2.0), `sv_grm_phone.lua` | **Радиосеть (RadioNet) v1.0.0:** инфраструктура вещания: СЕРВЕРНАЯ СТОЙКА (E = питание) — ядро сети и слабый репитер, АНТЕННА рядом с активной стойкой УСИЛИВАЕТ сигнал (круг покрытия антенны ≫ стойки), РАДИОПЕРЕДАТЧИК — стык «микрофон→сеть». Завязано: эфир микрофона до приёмников доходит только в покрытии сети и с «радио-искажением» (детерминированные выпадения голоса ∝ качеству сигнала + треск/щелчки — поток в чистой Lua не деформируем, эффект честный: PlayerCanHearPlayersVoice + звуки); новый режим микрофона ГРОМКАЯ СВЯЗЬ (доступ /alert_allow): голос звучит УСИЛЕННО из всех сетевых громкоговорителей с релейными щелчками; ручная рация ловится только в покрытии (вне сети — прямая дальность); /alert звучит только на громкоговорителях в сети; МЕГАФОН (SWEP) — голос ×4.5 дальности с эффектом рупора. Единая маршрутизация `GRM.RadioNet.VoiceRoute` (телефония консультирует первой — гонка pairs() хуков голоса убрана, находка 100); автоперсистентность `grm_rnents/<map>.json`; спавн `/rack_add` `/antenna_add` `/rstation_add` (+_remove), диагностика `/rn_status`; стенд sim_radionet (58 проверок) |
| — | `materials/entities/*.png` | **Иконки Q-меню:** 33 иконки в едином стиле для всех энтити и оружия GRM (автоподхват по имени класса) |
| 87 | `lua/autorun/sh_grm_radionet.lua` (v1.1.0) + `lua/entities/grm_net_console/` + правки `sh_grm_broadcast.lua` (v1.2.1) | **RadioNet NetSys — идентификация, группы, пеленг, журнал:** каждая железка сети получает позывной (RAX/ANT/RST/MIC/SPK/RAD/CON-001…) и живёт в реестре `grm_rnsys/<map>.json` (переживает рестарт «воскрешением» по позиции ≤32 юн). ПУЛЬТ РАДИОСЕТИ (`grm_net_console`, компьютер, работает рядом с АКТИВНОЙ стойкой; суперадмин): 4 вкладки — **Устройства** (позывной/тип/статус/позиция/дистанция/азимут/качество/группы; ПКМ — точечное вкл/выкл ВЫВОДА любой железки: антенна гаснет → её круг покрытия схлопывается, громкоговоритель замолкает, стойка вынимается из ядра; назначение в/из групп; микрофону — цель ГРОМКОЙ СВЯЗИ: его голос звучит только из громкоговорителей выбранной группы или всех), **Группы** (состав, удаление), **Оповещение** (/alert с пульта на конкретную группу громкоговорителей или на весь город), **Журнал+пеленг** (события сети: передачи эфира/громкой связи/мегафона, оповещения, переключения — кто/что/позиция/качество канала, глубина 150). Позывные рисуются над железками (<350 юн). Команды: /console_add/_remove, /rn_log. sim_radionet 114 проверок |
| 88 | `lua/autorun/sh_grm_mobile.lua` (v1.2.1) + `lua/entities/grm_mobile_line/` + правки `sv_grm_phone.lua`, `sh_grm_phone_shop.lua`, `sh_grm_inventory.lua` | **Мобильный телефон (модели GTA IV by ivancorn):** 7 трубок тирами (700→14000, чем дороже — тем слабее нужен сигнал: 0.35→0.10 и тем больше приложений). Покупка в `/phoneshop` (предмет в инвентарь, лимит 2 на тир); открытие **СТРЕЛКОЙ ВВЕРХ**, навигация ↑↓, ENTER — открыть/ответить, BACKSPACE — назад/трубка, цифры — набор. Приложения: Телефон, Калькулятор (все); +SMS/Контакты (badger); +Заметки (badger_touch/lost); +Биржа труда, Моя фракция (онлайн/офлайн, роли, лидер ★), городской Форум (tinkle/whiz). Линия `grm_mobile_line` живёт пока трубка в инвентаре: номера 5-значные (отдельно от городских), связь по покрытию RadioNet (нет сети — свободная связь), разговор рвётся при потере сигнала, трубка-проп в руке во время вызова. Звонки интегрированы в ядро телефонии (`sv_grm_phone`): город↔мобильный↔мобильный, прослушка `grm_phone_wiretap` ловит мобильные номера (голос+текст). **Репорт «чат закрыт» закрыт (Код 88.1):** звонки-призраки больше не глушат чат — дисконнект/удаление телефона/трубки брошены >3с → звонок завершается с уведомлением; застрявшие линии самолечатся в idle; RP-чат v1.1.0 отвечает «рядом никого нет» вместо тишины. **Код 88.2 — UI-модернизация (v1.1.0, находка 104):** дёрганая навигация убита в корне — системный авторепит клавиатуры (шторм PlayerButtonDown 20–40 Гц с плавающей частотой) заменён собственным репит-клоком (зажал стрелку: 0.45с пауза, дальше ровные шаги по 0.11с; антидребезг ест повторные Down без Up, страховка от залипания через input.IsKeyDown); плашка выделения плавно «подъезжает» к строке, все списки получили настоящую прокрутку окном + скроллбар (раньше обрубались на 13–14 строках). Дизайн перерисован под «современный смартфон»: корпус с бортом и обоями, silk-иконки приложений, экраны с заголовками, софт-кей бар, fade-переходы, подъём панели при открытии; звонилка — круглые кнопки со вспышкой нажатия; вызов — аватар-круг с инициалом и пульсом, зелёная/красная карточки действий; SMS диалогами-пузырьками с тредами по абонентам и именами из контактов; входящий поповер с пульсом. sim_mobile 107 + sim_mobile_ui 29 проверок. **Код 88.3 — живые багфиксы (находка 105, v1.2.0):** корень «мёртвых звонков» — несуществующая константа `NO_USE` в init.lua линии (Initialize умирал посередине, линия жила с пустым статусом → любой вызов «линия занята»): исправлено + пост-спавн страховка + самолечение старых линий тикером. Открытый телефон: сервер держит стойку игрока (StartCommand: нет ходьбы/стрельбы), слоты оружия заблокированы, **колесо мыши листает меню**, данные (контакты/заметки/SMS) автообновляются раз в 3с — клиент шлёт keepalive-ping, протухание снимается само. **Код 88.4 — перманентные вышки:** авто-свипер персистента в RadioNet (v1.2.0) и Broadcast (v1.3.0): стойки/антенны/передатчики/пульты/громкоговорители/микрофоны/приёмники сохраняются на карту при постановке ЛЮБЫМ способом (команда, Q-меню, дюп), переезд — миграция записи, удаление живьём — не воскресает. sim_mobile 121 + sim_mobile_ui 40 + sim_radionet 121 |
| 89 | `lua/entities/grm_alarm_speaker/` + `lua/autorun/server/sv_grm_alarm.lua` (v1.2.0), `lua/autorun/sh_grm_alarm_config.lua` (v1.1.0), `lua/autorun/client/cl_grm_alarm.lua`, `lua/autorun/server/sv_grm_cctv.lua` (v1.2.1), `lua/autorun/sh_grm_perm_entities.lua` (v1.2.0) | **Охранный стек: динамик сирены + сеть из терминала + перманентность везде (находка 106).** Новая энтити `grm_alarm_speaker`: воет сиреной, пока активна тревога в его сети (NetworkID = сеть блока коммутации), чекбокс «Динамик включён» в панели, 3D2D-лейбл, включается/снимается синком событий + сторожевым прогоном скан-тикера (патчи-сироты подчистаются). **Терминал охраны:** строка «Сеть терминала (NetworkID)» во вкладке «Управление» + в статусе «динамиков: N». **Автосейв персистента (Alarm и CCTV):** любая правка (подпись/сеть/вкл/радиус/FOV/режим/динамик) дебаунсом 1с пишется в сейв — правки больше не гибнут между кликом и рестартом. **Универсальный перм v1.2.0:** +grm_alarm_speaker, grm_keypad, grm_roomtap_chip/server/terminal, grm_ore_node/buyer, sent_vehicle_dealer; лимит 64→256; железо RadioNet/Broadcast сознательно не дублируется (свиперы Кода 88.4), временные классы отклоняются. sim_security 50 проверок. |
| 90 | `lua/autorun/server/sv_grm_logistics.lua` (v1.2.1), `lua/autorun/sh_grm_perm_entities.lua` (шапка) | **Логистика: перманентность по заказу-вопросу (находка 107).** Точки погрузки/склады/оружейные уже сохранялись с полным стоком (grm_logistics/maps/<map>.json), но cleanup-кнопка стирала их до рестарта — добавлен воскрешающий хук PostCleanupMap; складской сток (депозит/выдача/приём маршрута) жил в памяти до ShutDown — теперь автосейв дебаунсом 1с на мутациях стока. Команды ручного контроля: grm_logistics_save / grm_logistics_load. В универсальный перм классы логистики сознательно не добавляются (свой сейв + сток). |
| 91 | `lua/autorun/sh_grm_qmenu.lua` (v3.0.0) + `tools/luatest/sim_qmenu.lua` | **GRM Стройка+ — полная переработка урезанного Q (находка 108).** Когда ванильное Q игрокам закрыто (playersQ=false), Q открывает собственное меню: сайдбар (Каталог / Инструменты / Куратор* / Настройки*), живой поиск по каталогу и по инструментам (теперь с русскими описаниями), лимит «мои пропы N/cap» пушами сервера, тосты причин отказов, «убрать проп в прицеле» / «убрать все мои», меню-пропы в undo (Z) и cleanup движка, реестр снимается при дисконнекте, auto-close по ESC/смерти. Суперадмин собирает меню игрокам не выходя из него: кнопка «Засидеть базовой мебелью (+18, сервер сам фильтрует отсутствующие модели)», +из прицела, +по пути, удаление ПКМ, вкладка Настройки (playersQ / меню стройки / свободные модели / белый режим инструментов / защита мебели от remover — работает только на своих пропах / лимит до 500). Открытие: игрок Q, админ /qm; хабная вкладка «Инструменты» совместима (общий Cfg). sim_qmenu 66 проверок. |
| 92 | `lua/autorun/sh_grm_qmenu.lua` (v3.1.0) | **GRM Стройка+ — вёрстка «как ванильное Q» (находка 109).** По скриншотам владельца: инструменты вынесены в ПРАВУЮ колонку меню — сворачиваемые категории (Соединения / Механика / Свет и эффекты / Интерфейс / Оформление / Точность / Опасное), активный инструмент подсвечен и выбор больше НЕ закрывает меню (выбрал — и строишь, как в песочнице), закрытые для игрока показаны затемнёнными с честным тостом причины, пустые для игрока категории скрыты. Каталог — карточки 96×118 с коротким именем модели и тултипом полного пути (ПКМ в кураторе — удалить). Шапка показывает «группа: user · Фракция (Ранг)» (GetUserGroup + FactionsData с sid/s64-ключами); футер — тулган взять/убрать, проп в прицеле, все мои + «Мои пропы N/cap» + активный инструмент + тосты отказов. Размер окна адаптивен под экран (960–1180 × 620–780, guards — стенд на ANY-стабах в норме). ToolCatalog получил категории (`cat`) + публичный `QM.ToolCategories` — хаб «Инструменты» совместим (читает только id+label). Сервер и протоколы Кода 91 не тронуты, sim_qmenu 66 без правок. |
| 93 | `lua/autorun/sh_grm_qmenu.lua` (v3.2.0) + `tools/luatest/sim_qmenu.lua` | **Стройка+ — антиперехват и диагностика (находка 110).** Живой сервер владельца показал: окно Q открывает ЧУЖОЙ аддон «GRM Restricted Q Menu» (в сборку не входит — ни `lua/`, ни legacy-архивы его не содержат), он глушит `SpawnMenuOpen` раньше нас, поэтому новый визуал v3.1.0 был невидим. Модуль научился себя защищать диагностикой: авто-сенсус через 6 с после загрузки клиента — если `SpawnMenuOpen`/`OnSpawnMenuOpen`/`ContextMenuOpen` держат чужие хуки, в консоль падает предупреждение с их id; `/qm_diag` (суперадмин) или консольная `grm_qmenu_diag` — полный дамп пяти Q-ивентов с пометкой «ЧУЖОЙ» (по id сразу видно аддон-виновник). Новый флаг `adminsToo` (дефолт false, чекбокс в «Настройках», SetOpt, грузится/сейвится): суперадмину тоже «Стройка+» вместо ванильного Q — предпросмотр меню игрока без второго аккаунта. sim_qmenu 69 проверок, GLua 248/248, стенд 14/14, симы 12/12, proto чист. |
| 94 | `lua/autorun/sh_grm_qmenu.lua` (v3.2.1) | **Стройка+ — крупнее и без наслоений (находка 111).** По живому скриншоту владельца: окно стало заметно больше и шире — 72% ширины и 82% высоты экрана, пределы 1080–1360 × 680–900 (было 960–1180 × 620–780); футер пересобран: высота 64px, кнопки 30px с шагом 8px в один чистый ряд (Взять/Убрать тулган 124, проп в прицеле 184, все мои 140), отделяющая линия, строка «Мои пропы N/cap | Инструмент | тост» отведена вниз без прижатия к кнопкам и краю рамки; шапка правой колонки инструментов 30px, скролл ниже. Сервер не тронут, протоколы прежние — sim_qmenu 69 без правок, GLua 248/248, стенд 14/14, симы 12/12, proto чист. |
| 95 | `lua/autorun/sh_grm_qmenu.lua` (v3.3.0) + `tools/luatest/sim_rootboard.lua` | **Стройка+ — hold-Q и починка колонки инструментов (находка 112).** Наслоение инструментов было не футером: строки правой колонки создавались БЕЗ `Dock(TOP)` — `DScrollPanel:AddItem` родит, но не докует, поэтому вся колонка лежала стопкой в одной точке с нулевой шириной; `scrollAdd` теперь докует TOP сам — категории и строки распределились по высоте. Поведение Q приведено к стандартному: **меню живёт, пока зажата Q** — `PlayerBindPress(pressed=true)` открывает, `pressed=false` закрывает (`QM.CloseMenu`); `OpenMenu` перестал быть переключателем (повтор при удержании игнорируется), `/qm` сохранён как toggle на ресивере; крестик/ESC/смерть закрывают как раньше (OnRemove снимает ссылку). sim_rootboard: ожидание «отпускание = nil» заменено на «отпускание = true». GLua 248/248, стенд 14/14, симы 12/12, proto чист. |
| 96 | `lua/autorun/sh_grm_qmenu.lua` (v3.3.1) | **Стройка+ — ещё крупнее (находка 113).** По просьбе владельца окно расширено до 80%×88% экрана (пределы 1200–1560 × 720–980; на 1920×1080 ≈ 1536×950) — больше карточек в ряд, длиннее колонка инструментов. GLua 248/248, стенд 14/14, симы 12/12, proto чист. |
| 109 | `lua/entities/grm_keypad/init.lua`, `lua/entities/grm_scanner/init.lua`, `lua/autorun/sh_grm_ffdlink.lua` (v1.1.0), `lua/weapons/gmod_tool/stools/ffd_link.lua`, `lua/autorun/sh_grm_inventory.lua` (v1.5.0), `lua/autorun/zz_grm_food_inventory_patch.lua` (v2), `lua/autorun/sh_grm_radionet.lua` (v1.4.2), `lua/autorun/client/cl_grm_inventory_ui.lua`, `tools/luatest/sim_ffdtools.lua` (144), `tools/luatest/sim_invphone.lua` (41) | **Конец авто-связи контроллеров + рация ожила: «Использовать» по модулятору снова переключает (Код 109, два заказа владельца, находка 126).** (1) **FFD: кейпад и сканер больше НЕ определяют двери сами.** Радиусный фолбэк «нет связей → открыть всё в 250» (оставленный в Коде 108 ради совместимости) владелец заказал убрать полностью: контроллер без ручных связей FFD Link теперь НЕ ОТКРЫВАЕТ НИ ОДНУ ДВЕРЬ — грант-табло и нумпад-сигнал живут, двери не трогаются, владельцу уходит видимая подсказка «привяжите инструментом FFD Link» (раз в 5с). Умерла и скрытая ловушка «умерла последняя связь → снова весь радиус». (2) **Рация — нормальный модуль, /r /freq работают как задумано.** Корень «в инвентаре жму Использовать — ничего» найден и вырван: `zz_grm_food_inventory_patch.lua` ЗАМЕНЯЛ `net.Receive("grm_inv_use")` своей неполной копией (еда/оружие/патроны/heal/armor) — клики по МОДУЛЯТОРУ (а заодно по кошельку, телефону и медкарте) умирали беззвучно, `data.on` никогда не включался, поэтому `/freq` `/r` вечно отвечали «нет активного модулятора» и после рестарта тоже. Инвентарь v1.5.0: useFunc-диспетчер пересел на РЕЕСТР `GRM.Inventory.RegisterUseHandler` — расширение через API, замена ресивера упразднена; useFunc без обработчика = ВИДИМЫЙ отказ игроку + строка в консоль (эпоха «мёртвых кнопок» закрыта). Патч еды v2 регистрирует `grm_food_eat` через API и ничего не заменяет. Бонус: в деталях модулятора видно [ВКЛ]/[ВЫКЛ]. sim_ffdtools 140→**144** (запрет авто-связи кейпад+сканер, подсказки владельцу), sim_invphone 27→**41** (живая регрессия заказа: патч еды после инвентаря → модулятор живёт toggle-ом, медкарта жива, еда естся через реестр, чужой useFunc доезжает, без обработчика — видимый отказ). GLua 253/253, roundtrip 11/11, симы 15/15, proto чист, dist пересобран. |
| 108 | НОВЫЙ `lua/autorun/sh_grm_ffdlink.lua` (v1.0.0), НОВЫЙ `lua/weapons/gmod_tool/stools/ffd_link.lua`, `lua/entities/grm_keypad/init.lua`, `lua/entities/grm_scanner/init.lua`, `lua/weapons/gmod_tool/stools/ffd_fading_door.lua`, `lua/autorun/sh_grm_perm_entities.lua` (v1.4.1), `lua/autorun/sh_factions.lua`, `tools/luatest/sim_ffdtools.lua` (140), НОВЫЙ `tools/luatest/sim_factions_live.lua` (30) | **Механика ручной связи FFD-контроллеров с дверями + «Основной» отдел больше не воскресает + «живые» вкладки /factions (Код 108 по заказу владельца, находка 125).** (1) **FFD Link:** раньше кейпад/сканер открывали ВСЕ FFD-двери в радиусе 250 — нельзя было задать «этот замок → та дверь». Теперь связь задаётся вручную новым инструментом **FFD Link (связь)** (Q → Инструменты → GRM): ЛКМ по кейпаду/сканеру — выбор контроллера (повторный ЛКМ — сброс), ЛКМ по двери — связать/отвязать, ПКМ по контроллеру — снять все его связи, ПКМ по двери — отвязать её ото всех контроллеров, R — сброс. Связи есть → открываются ТОЛЬКО привязанные двери; нет связей → старое радиус-поведение (совместимость). Хранилище — `GRM.FFDLink`: записи класс+позиция (округление 0.1, допуск разрешения 1.2 юнита — иначе соседняя дверь подменяла бы удалённую), самоочистка prune, NW-зеркало `FFDLinkN/FFDLinkIdx` для клиентской подсветки (`TOOL:DrawHUD` дёргается из `SWEP:DrawHUD` тулгана — проверено по исходнику gmod_tool), дубликат-модификатор `FFD_LinkList`, перм v1.4.1 — связи едут в `rec.data.links` кейпада/сканера и воскресают после рестарта. FFD-стул получил NW-метку `FFD_IsDoor` и авто-отвязку двери из всех контроллеров при снятии статуса. (2) **«Основной» отдел больше никогда не создаётся сам при создании/удалении отделов:** корень — `ensureDefaults` вставлял его при КАЖДОМ вызове, а вызывается он из каждого действия и каждой SYNC-рассылки — стоило удалить, как sync возвращал его назад. Автовставка убрана; дефолтный отдел участников — первый реальный (`getDefaultDepartment`), «Основной» остался лишь стартовым отделом свежей фракции. (3) **«Живые» вкладки /factions без перезапуска меню:** админские «Ранги»/«Отделы»/«Список» и комбо ролей/отделов (админ и лидер) раньше строились один раз при выборе фракции; теперь ЛЮБАЯ SYNC_ALL-рассылка (своё действие, другой админ, лидер) сразу перестраивает текущие вкладки по свежим данным, выбор в комбо сохраняется, а мёртвый — гасится явно. sim_ffdtools 94→**140**: связка-ядро (дубль/toggle/NW), грант по связи открывает только привязанную дверь (соседняя в 3 юнитах — нет!), радиус-фолбэк, prune с защитой от подмены соседней дверью (допуск 1.2), RemoveFromAll, Export/Import, перм-связи после «рестарта», стул: выбор/связки/ПКМ-очистки/R/Holster/вежливые отказы/CPanel/DrawHUD с подсветкой «СВЯЗАНА». sim_factions_live **30/30**: «Основной» не воскресает ни в памяти, ни в SYNC-пакете, ни после «рестарта»; дефолт первого реального отдела; живые вкладки/комбо по рассылке; плюс `continue` в sh_factions заменён ванильным Lua (стенды парсят файл напрямую). GLua 253/253, roundtrip 11/11, симы 15/15, proto чист, dist пересобран. |
| 107 | НОВЫЙ `lua/entities/grm_scanner/` (shared+init+cl_init), НОВЫЙ `lua/weapons/gmod_tool/stools/ffd_scanner.lua`, `lua/entities/grm_keypad/init.lua`, `lua/weapons/gmod_tool/stools/ffd_keypad.lua`, `lua/autorun/sh_grm_perm_entities.lua` (v1.4.0), `tools/luatest/sim_ffdtools.lua` (94) | **FFD Scanner + кейпад становится чистым PIN-замком (Код 107 по заказу владельца + хотфикс «верный PIN → отказ», находка 124).** (a) **Хотфикс PIN**: три невидимых прежде причины «правильного кода, но отказа» — пароль из поля ввода мог лежать в конваре с хвостовым пробелом (раньше маскировал байпас владельца), анти-дабл 0.15с съедал цифры быстрого набора, кап ввода 6 обрубал длинные PIN. Теперь: пароль и ввод ТРИМЯТСЯ при сравнении и при установке, анти-дабл 0.06с, кап 10 цифр, «насыщение поля» даёт звук. (b) **Кейпад — только PIN-режим**: комбо режимов, окно фракций и слайдер платного прохода убраны из панели и энтити (режим принудительно 0 даже из старой перм-базы). (c) **Новый FFD Scanner** (`grm_scanner` + стул `ffd_scanner` в Q → Инструменты → GRM): панель БЕЗ ввода кода — человек подходит и жмёт [E] глядя на сканер → 0.9с анимация сканирования (бегущая полоса на 3D2D-табло) → решение СТРОГО по его фракции из белого списка (как и у кейпада, байпасов владельца/админа нет — находка 123: байпас ослепляет самого тестирующего): допущен — имя и фракция на табло, FFD-двери рядом открываются на заданные секунды, нумпад-сигнал владельцу; отказ — красная вспышка. Окно белого списка фракций — то самое интерактивное окошко из Кода 106 (живой кэш FactionsData, чекбоксы, авто-перестройка по синку). Геометрия 3D2D — доказанный базис AngleEx+OBB. Перм v1.4.0: сканер сохраняется через /permadd с фракциями/сигналами/задержкой/владельцем и воскресает готовым. sim_ffdtools 94/94: трим PIN, кап 10, анти-дабл по часам, PIN всегда решает (даже при легаси-режиме 1), полный цикл сканера медик/чужак/владелец-без-фракции, стул сканера (спавн/фриз/notify), перм сканера с воскрешением и рабочим сканированием. GLua 252/252, симы 14/14, roundtrip 11/11, proto чист, dist пересобран. |
| 106 | `lua/entities/grm_keypad/init.lua`, `lua/weapons/gmod_tool/stools/ffd_keypad.lua`, `lua/autorun/sh_grm_inventory.lua` (v1.4.0), `lua/autorun/sh_grm_radionet.lua` (v1.4.1), `lua/autorun/sh_grm_medical.lua`, `lua/autorun/sh_grm_mobile.lua`, `tools/luatest/sim_ffdtools.lua` (70), `tools/luatest/sim_invphone.lua` (27) | **Кейпад: строгий PIN и интерактивное окно фракций + воскрешение модулятора рации (находка 123, 3 замечания владельца).** (1) **«Не различает правильный и неправильный пароль»:** байпасы владельца/суперадмина (Код 104/105) пропускали ЛЮБОЙ ввод — владелец сервера тестирует со своего суперадмин-аккаунта и не мог увидеть разницу. PIN-режим теперь строгое сравнение для ВСЕХ (включая пустой ввод ≠ пароль); байпас остаётся только в фракционном. Побочная добыча: строгость разоблачила баг самого сима — цепочка «1-2-3-4» жала кнопку [10]=CLR вместо [4] («4») и проходила только байпасом; исправлено. (2) **«Окна с фракциями нету, чисто окно ввода, нужна интерактивность»:** корень — серверный глобал `Factions` на клиенте равен НИЛ, живой список лежит в клиентском кэше `FactionsData` (синк `Factions_SyncAll` из sh_factions). Панель читает `FactionsData` (приоритет) → `Factions` (легаси); окошко строится ВСЕГДА и само перестраивается по Think-подписи раз в 0.5с: данных нет — ручное поле `DTextEntry`, синк пришёл — чекбоксы на каждую фракцию без переоткрытия. (3) **«Модулятор в инвентаре не срабатывает»:** дефом владел только RadioNet внешней регистрацией с ретраем ВНУТРИ гарда на `GRM.Inventory` — перевёрнутая загрузка гасила и ретрай, деф не появлялся никогда, `useItem` молча выходил («мёртвая кнопка»). Теперь: **статический деф `radio_modulator` прямо в ItemDefs инвентаря** (v1.4.0, перезаписывается RadioNet тем же содержимым), гард у регистраторов RadioNet/медкарты/телефонов — ВНУТРИ функции (паттерн 99 правильный), а useItem без тихих выходов: нет дефа → видимый отказ игроку + строка в сервер-консоль. sim_ffdtools 66→**70** (FactionsData-репродукция живого бага, строгий PIN: владелец/суперадмин с неверным PIN → deny, верным → grant, пустой → deny), sim_invphone 25→**27** (статический деф до внешних регистраций, призрачный предмет → «не зарегистрирован»). GLua 249/249, roundtrip 11/11, симы 14/14, proto чист, dist пересобран. |
| 105 | `lua/weapons/gmod_tool/stools/ffd_keypad.lua`, `lua/entities/grm_keypad/` (shared+cl_init+init), `lua/weapons/keypad.lua`, `lua/weapons/gmod_tool/stools/ffd_fading_door.lua`, `lua/autorun/sh_grm_perm_entities.lua` (v1.3.0), `tools/luatest/sim_ffdtools.lua` (66) | **Кейпад: поле PIN в панели, ровный 3D2D-экран и ПЕРМ кейпада+двери (находка 122, 3 замечания владельца).** (1) **В панели тулгана появилось поле «Пароль (PIN-код)»**: голый `AddControl("TextEntry")` — несуществующее ИМЯ контрола молча пропускалось без краша (замечание владельца «нету поля чтобы задать пин-код»); теперь живой DForm-хелпер `panel:TextEntry(label, convar)` с фолбэком `AddControl("TextBox")` — и для PIN, и для фолбэка фракции. (2) **3D2D-экран больше не уезжает и не повёрнут на 180°**: урок находки — не собирать угол плоскости парами RotateAroundAxis «наугад» (пара (Right,−90)/(Up,−90) дала ролл 180°). По формуле вики pixel→world экран-X = ang:Forward, экран-Y(вниз) = ang:Right, нормаль = ang:Up — угол строится НАПРЯМУЮ из базиса модели одной строкой `(-self:GetRight()):AngleEx(self:GetForward())`; масштаб панели — не константой, а по реальной морде модели через `OBBMins()/OBBMaxs()` (`KeypadScreenFrame`: ширина/высота грани → `min(w/144, h/220)`), фолбэк-пад на константы сохранён. (3) **Админский ПЕРМ с данными экземпляра**: `sh_grm_perm_entities` v1.3.0 — реестр `GRM.PermData.Extract/Apply[class]`; `/permadd` пишет `rec.data` (pcall Extract), спавн после рестарта/`/permload` разворачивает его обратно (pcall Apply). Кейпад переживает рестарт **готовым к работе**: PIN/режим/цена/список фракций/кнопки сигналов/задержка/владелец (sid64 → `IsKeypadOwner`, ищет онлайн-плеера); **prop_physics допущен в пермы ради FFD-дверей** — `/permadd` по fading door пишет `rec.data.ffd` (ключ/инверсия/тогл/автозакрытие/время/владелец), после рестарта проп СРАЗУ рабочая дверь (`GRM.FFD_MakeFadingDoor` = core-фабрика стула; страховка: `numpad.OnDown/OnUp` не дёргается без живого владельца — NULL-плеер роняет движок). sim_ffdtools 41→**66**: TextEntry-моки и счётчики панели, OBB-масштаб экрана, базис 3D2D (F=−E:R / R=−E:U / U=+E:F), полный перм-цикл «добавить → рестарт → воскресить с данными → дубль/снять» для кейпада и двери. GLua 249/249, roundtrip 11/11 фаз, симы 14/14, proto чист, dist пересобран. |
| 104 | `lua/entities/grm_keypad/` (shared+init+cl_init v1.1.0), `lua/weapons/gmod_tool/stools/ffd_keypad.lua`, `lua/weapons/keypad.lua` (v1.1.0), `tools/luatest/sim_ffdtools.lua` (41) | **Кейпад: фракции чекбоксами, правильный спавн и экран «как у модов» (находка 121, 3 замечания владельца).** (1) В панели тулгана — окошко ФРАКЦИЙ С ЧЕКБОКСАМИ (можно несколько, список через запятую в `ffd_keypad_faction`; кейпад разбирает список `IsFactionAllowed`, владелец/суперадмин — мимо). Пустой глобал Factions — фолбэк текстового поля. (2) **Спавн кейпада набок — убраны лишние повороты:** модель `props_lab/keypad` смотрит лицом в +X, правильно `ent:SetAngles(HitNormal:Angle())` БЕЗ RotateAroundAxis (тулган и SWEP; регресс-страж в симе). (3) **Экран 3D2D переписан:** пара поворотов плоскости (Right,−90)+(Up,−90) — старая (0,90,90) ЗЕРКАЛИЛА текст; геометрия (ориджин/угол/хит-тест кнопки по мировой точке) общая в shared. **Кнопки нажимаются ПРИЦЕЛОМ + E** (как в модовых кейпадах): сервер сам считает кнопку по GetEyeTrace, общий [E]=OK убран (двойные срабатывания); подсветка кнопки под прицелом, **вспышка нажатой кнопки у ВСЕХ клиентов** (net `GRM_KeypadPress`), плавный лерп цвета статуса, мигающий курсор поля ввода. sim_ffdtools 21→**41**: конвер фракций сортированный список/инициализация из конвара, хит-точки кнопок (ось не зеркальна), E-цепочка PIN 1234 → grant, вспышка-протокол, список фракций grant/deny/владелец, анти-двойное E. GLua 249/249, стенд 14/14, симы 14/14, proto чист (новый симметричный `GRM_KeypadPress`). |
| 103 | `lua/weapons/keypad.lua` (v1.0.0), `tools/luatest/sim_ffdtools.lua` | **Классический кейпад-SWEP + патч патриарховских FFD-крешей (находка 120).** По заказу «keypad lua как у кейпад-модов»: в руках — оружие `keypad` (Q → Оружие → GRM SWEP, модель тулгана), ЛКМ ставит `grm_keypad` (спавн+фриз+undo, PIN 1234), ПКМ снимает свой (суперадмин — любой, чужой защищён). В отличие от стула: у каждого действия фидбек (звук + GRM.Notify — стул молчал); анти-спам кап 24 своих кейпада. **Патч находки:** у FFD-свепов владельца `SWEP:ViewModelDrawn` крешился, когда хук звался «dot-стилем» `ViewModelDrawn(vm)` (self=вм, vm=nil → nil-call на vm:GetModel) — наш кейпад принимает ОБА стиля вызова (гейт по типу/IsValid/GetModel). sim_ffdtools: спавн-поля/фриз/undo/звук/notify, кап 24, гейты ПКМ (свой/чужой/админ), 3 стиля ViewModelDrawn. GLua 249/249, стенд 14/14, симы 14/14, proto чист. |
| 102 | `lua/weapons/gmod_tool/stools/ffd_keypad.lua` | **FFD Keypad: панель инструмента падала целиком (находка 119, по ошибке владельца `SetDock (a nil value)`).** В BuildCPanel рукомесный `vgui.Create("DComboBox")` звал НЕСУЩЕСТВУЮЩИЙ метод `combo:SetDock` — исключение на строке 123 выкидывало всю панель настроек тулгана (режимы PIN/фракция/платный были недоступны). Заменено на стандартный `panel:ComboBox(label, convar)` DForm: корректный док + синхронизация конвара (при переоткрытии выбор восстанавливается). Аудит остальных стулов GRM чист (все через AddControl/хелперы), единственный SetDock в репо был этот. sim_ffdtools ловит регрессию: vgui нарочно не объявлен — старый код упал бы на месте. |
| 101 | `lua/autorun/sh_grm_medical.lua` (v1.1.0), `lua/autorun/sh_grm_inventory.lua` (v1.3.0), `tools/luatest/sim_medical.lua` (76), `tools/luatest/sim_invphone.lua` (25) | **Медкарты НА РУКИ (заказ владельца, находка 118).** Врач в редакторе карты (/medcards → пациент) жмёт «Выдать карту на руки» — пациенту падает в инвентарь предмет «Медицинская карта» (модель `models/props_lab/clipboard.mdl`, фолбэк `paper01.mdl` по н85): выбрасывается на землю моделью, подбирается, **sid64 владельца живёт в данных самого предмета** — дроп, подбор чужими руками и рестарт сервера привязку не теряют. «Использовать» в `/inv` открывает держателю просмотр карты владельца (RP-логика физического носителя: держишь — читаешь/предъявляешь; править по-прежнему только персонал с доступом). Защиты выдачи: пациент офлайн — отказ; себе — отказ (напоминание про /mycard); **повторная выдача закрыта, пока карта в инвентаре пациента**; нет места — отказ. Факт выдачи пишется В КАРТУ (поле `issued`: врач/sid64/время) + служебная запись журнала вида «Выдача карты» — переживает рестарт; вручную такую запись добавить нельзя (серверный отказ + вида нет в комбо формы). Пациент получает уведомление, врачу приезжает свежая карта. Техника: Inventory v1.3.0 — новый useFunc `medcard_view` (предмет НЕ тратится, вызов `MD.ViewIssued(ply, slot.data)`); регистрация предмета паттерном Кода 99 (прямой вызов + retry `timer.Simple(2)`). sim_medical 56→**76** проверок: регистрация предмета, отказы не-медику/офлайн/себе/дубликату, sid64 в данных предмета, журнал выдачи с удержанием капа 60, пуш карты держателю, пустой бланк, персистентность `issued`. sim_invphone 20→**25**: useFunc → ViewIssued с теми же данными экземпляра, предмет цел, бланк с nil-data доходит. GLua 248/248, стенд 14/14, симы 13/13, proto чист. |
| 100 | `lua/autorun/sh_grm_mobile.lua` (v1.2.2) + `tools/luatest/sim_mobile_ui.lua` | **Телефон: «выбор скачет при прожатии» — закрыто двумя корнями (находка 117).** (1) На части клиентов (SDL) OS-авторепит клавиш идёт ПАРАМИ Down+Up: флаг `M.down` (Код 88.2) такой шторм не видит — каждая пара засчитывалась за нажатие, стрелки крутили выбор на 25–40 Гц. Лечение физическое: повторный Down принимается, только если клавиша РЕАЛЬНО отпускалась (гейт `stormHold`; «виртуальный» Up из пары при физически зажатой клавише удержание не рвёт — репит-клок продолжает работать и у штормящих систем, сторож Think добирает отпускание при кадровом лаге состояния). Человеческие быстрые тапы не страдают — между ними клавиша честно отжата. (2) Кольцевой перепрыг списка (последняя→первая строка) — плашка выделения больше не «проезжает» весь список анимацией, а снапится сразу на место (порог 1.5 строки). sim_mobile_ui 44 (+4: шторм 15 пар принят ровно один шаг / человеческий темп 0.09с все шаги живы / снап плашки 1→8 и 8→1 по геометрии отрисованного бокса). GLua 248/248, стенд 14/14, симы 13/13, proto чист. |
| 99 | `lua/autorun/sh_grm_radionet.lua` (v1.4.0), `lua/autorun/sh_grm_inventory.lua` (v1.2.0), `lua/autorun/sh_grm_phone_shop.lua`, `lua/entities/grm_item_drop/init.lua` | **Радиочастоты — только с переносным модулятором рации (находка 116).** По заказу: в /phoneshop появился товар «Модулятор рации (переносной)» (1500, модель `models/props_lab/reciever01b.mdl` с фолбэком reciever01a по н85) — кладётся в инвентарь, выбрасывается на землю своей моделью и подбирается. Доступ к `/freq` и `/r` = предмет В ИНВЕНТАРЕ + АКТИВИРОВАН («Использовать» — переключатель ВКЛ/ВЫКЛ, предмет не тратится); состояние живёт в данных самого предмета (`slot.data.on`) — переживает рестарт (сейв инвентаря н114), падает в дроп и возвращается при подборе чужими руками (включённая рация теряется/продаётся включённой). Для этого AddItem получил 4-й аргумент `data`, а `grm_item_drop` научился возвращать данные экземпляра при подборе (раньше подбор не-оружийных предметов терял data). Мёртвый кейс: выбросил модулятор — `/r` отвечает подсказкой, частота при этом помнится; `/freqleave` доступен и без железки; голый `/freq` честно говорит «модулятор ВЫКЛ». sim_radionet 183 проверки (+секция 25: гейт по наличию/активации/дропу), sim_invphone 19 (+секция 7: тоггл → дебаунс-сейв → жёсткий рестарт → предмет включён; подбор с data ложится своим состоянием). GLua 248/248, стенд 14/14, симы 13/13, proto чист. |
| 98 | `lua/autorun/sh_grm_radionet.lua` (v1.3.0) + `tools/luatest/sim_radionet.lua` | **Рация /freq /r ожила — обработчика команд не было вообще (находка 115).** C-меню (кнопка «Рация», Код — sh_grm_ctx) слало «/freq 145.5» и «/freqleave» в чат, но ни один модуль сборки их не парсил (DarkRP-радио в сборке тоже нет) — команды висели пустым текстом в общем чате. Реализовано в RadioNet: `/freq <1–999.9 шаг 0.1>` настройка частоты (память за sid64 до явного `/freqleave`, `/freq off` и `/freq 0` тоже отключают — C-меню шлёт именно их), голый `/freq` — напоминание, `/r <текст>` — эфир: слышат все на ТОЙ ЖЕ частоте по правилу ручной рации `RadioPairOK` (оба абонента в покрытии сети — хоть через карту; вне сети — только прямая дальность 1500 юн), на прямой дальности текст идёт **с помехами** (символы съедает шипение «*» ∝ удалению, кириллица не ломается — посимвольный utf8-путь), собственное эхо отправителю, «в эфире тишина» когда никого не слышит, антифлуд 1.5 с, кап 220 символов, `!r`/`!freq`-варианты, журналирование передач в `/rn_log` (вид `freq_say`), строка абонентов в `/rn_status`, восстановление частоты при перезаходе (PlayerInitialSpawn). Предохранители: `/radio`, `/report` и прочие чужие slash-команды не поглощаются. sim_radionet вырос 121 → **165 проверок** (новая секция 24: валидация частот, доставка по покрытию/прямой дальности, скремблер, антифлуд, отключение, воскрешение, журнал). GLua 248/248, стенд 14/14, симы 13/13, proto чист. |
| 97 | `lua/autorun/sh_grm_inventory.lua` (v1.1.0) + `tools/luatest/sim_invphone.lua` | **«Пропадают купленные телефоны после рестарта» — корень найден и вырван (находка 114).** Телефон был лишь первой жертвой: лоадер инвентаря читал `grm_inventories.json` голым `util.JSONToTable` — тот конвертировал числовые строковые ключи в числа, sid64 «7656…» становился битым double, и после перезагрузки ВСЯ карта инвентарей сиротела (игрок по строковому sid64 не находил свою запись → чистые 24 слота). Сим-репродукция подтвердила до фикса (FAIL 7–8). Лечение: 3-й аргумент JSON (н65), нормализация ключей слотов в числа, **sid64-rescue** (ленивое самолечение уже побитых сейвов: единственный кандидат в радиусе двойной точности — чужой инвентарь никогда не отдаём), **дебаунс-автосейв 2с** на ЛЮБОЙ мутации (покупка/выброс/перенос/сплит/экип — окно 10-сек автотаймера закрыто), read-back SAVE-печать. sim_invphone 13 проверок: покупка → файл → жёсткий рестарт → дебаунс → легаси-файл с битым ключом воскрешает телефон. Аудит прочих bare-JSON лоадеров сборки: массивы/именные ключи — вне опасности. GLua 248/248, стенд 14/14, симы 13/13, proto чист. |
| 86 | `lua/autorun/sh_grm_medical.lua` + правки `sh_grm_economy.lua` (штрафы), `sv_grm_logistics.lua` + `cl_grm_faction_logistics.lua` (диагностика), `sh_grm_ctx.lua` (деньги в C-меню), `sh_grm_qmenu.lua` (v2.0.0 GRM-стройка) | **Медицинские карты v1.0.0 + заказ 5-в-1:** врачи ведут полные карты пациентов — диагнозы (актив/излечён toggle), записи приёмов, назначения, операции, показания (кровь из 8 валидных, аллергии, хроника), кап 60 записей, пациент видит свою карту сам; ДОСТУП по фракциям/рангам/отделам через Factions (fail-closed, s64-фолбэк н101, UI «Доступ» суперадмину в /medcards) — /medcards /mycard + grm_medcards. ШТРАФЫ починены в корне: экономика искала члена фракции только по SteamID-ключу, патч memberRec (sid ИЛИ s64) во всех lookup'ах (находка 101); /fine ником или по прицелу, НОВАЯ /fines — статус прав и последние 5 штрафов фракции. ЛОГИСТИКА: simfphys-спавн вне дилера теперь распознаётся (VehicleName-ключ), /logistics_start объясняет ТОЧНУЮ причину отказа + grm_logistics_debug; пустые точки — окно-подсказка. Q-МЕНЮ v2.0.0: при playersQ=false Q открывает «GRM Стройку» — каталог одобренных админом пропов и выдача разрешённых инструментов вместо ванильного spawnmenu (абуз исключён: бинд глушится до аддонов, серверные гейты давят обход) — /qm_prop_add прицелом /_addmodel /_del /_list /_clearprops. C-МЕНЮ: кнопки «Выбросить деньги…» и «Передать деньги: Имя» (цель в прицеле до 3 м, GRM_Ctx_MoneyAct с антифлудом) |

**Освежение v4 (подсказки у машин + «Расширенные настройки»):**
- **Подсказки у транспорта при прицеле вблизи** (220 юн, `cl_vehicle_hud.lua`): под владельцем/замком — строка управления «Ключи: ЛКМ/ПКМ — замок • /trunk — багажник»; у открытого багажника — янтарное «● БАГАЖНИК ОТКРЫТ — кладите/забирайте в окне • /trunk — захлопнуть». У «диких» машин без владельца теперь тоже видна подсказка (раньше HUD молчал).
- **«Расширенные настройки» /factions переработаны и синхронизированы** (sh_faction_fixes v3.1.1): вкладка вернулась (была мёртвая обезьянья заплата — заменена на хук `GRM_FactionsAdmin_BuildTabs`), показывает живые счётчики моделей/оружия (по фракции/ролям/отделам), статус ком.часа с таймером и кнопкой отмены, отделы маскировки, доступ /gnews; авто-обновление при любых изменениях (1.5 с). Серверное зеркало `buildSyncData` теперь шлёт модели/оружие/gnews; оружейные списки наконец сохраняются на диск (раньше слетали после рестарта).

**Освежение v3 (Коды 77/79/80 + аудит кнопок):**
- **Аудит работоспособности кнопок** (инструмент `tools/proto_audit.py` — сверка net.Start ⇄ net.Receive по всей сборке, whitelist внешних протоколов; прогон чист, добавлен в регресс): найдено и исправлено 5 дефектов:
  1. **ТАБ → «Спавн транспорта»** слал сообщения в никуда (приёмников не было) — добавлен мост в `sent_vehicle_dealer/init.lua` (`VD_RequestVehicleList`/`VD_AdminSpawnVehicle`): список ближайшего дилера, спавн без цены и лимита личных машин, суперадмин-гейт;
  2. **телефонная синхронизация** `GRM_Phone_Sync` не имела клиентского приёмника (тихий обрыв при звонках с таксофонов/АТС) — добавлен толерантный приёмник в `cl_grm_phone.lua`;
  3. **«Сохранить» в доступе к дверям** (`/door_access`) срабатывало только у суперадмина, а матрица категорий открывается выданным управляющим (`AM.CanManage`) — выданные управляющие жали «Сохранить» вникуда; гейт выровнен (`CanManage`);
  4. **вакансия биржи умирала при провале исполнителя** — многоразовая вакансия теперь только снимает бронь, смены остаются; отзыв занятой вакансии корректно возвращает остаток эскроу (сим-тесты);
  5. **Отображение «Comedy Effect»** — стороннее имя из конфига ключей больше не рисуется над машинами (`cl_vehicle_hud.lua`) — остались «Владелец» и статус замка.
- **Биржа v1.1.0 (вакансии)**, **админ-хаб `/grm_admin` (Код 79)**, **багажник любого транспорта (Код 80)** — подробно в рядах таблицы выше.

**Освежение v2 (в составе Кода 11):** `/models_admin` и `/weapons_admin` — живое превью модели (DAdjustableModelPanel, клик по строке), SpawnIcon в строках, каталог оружия с поиском и категориями (выбор кликом из всех SWEP'ов сервера), инфо-панель скина/бодигрупп.

## Основные команды

**Игрок:** `/inv`, `/drop`, `/store`, `/fjoin`, `/fleave`, `/fr`, `/dep`, `/depb`, `/mask`,
`/freq <1–999.9>`, `/freqleave`, `/r <текст>` (радиоканал: эфир ВСЕМ на той же частоте в досягаемости сети/прямого сигнала; **нужен активный модулятор рации из /phoneshop — включён кнопкой «Использовать» в /inv**),
`/model`, `/gnews`, `/kom_hour`, `/me`, `/do`, `/it`, `/try`, `/roll`, `/w`, `/y`, `/looc`,
`/logistics_start`, `/logistics_crates`, `!fbudget`, `!fpay`, `!fwithdraw`, `!fpayall`, `!fsettax`, `/mysalary`, `/fine <сумма> [причина]`, `/vlist`, `/myvehicles`,
`/vshop`, `/phoneshop`, `/phone_remove`, `/alert <текст>`, `/alertall <текст>` (по доступу), `/name`, `/char`, `/rpdesc`, `/menu`,
`/dropmoney`, `/money_pack` (деньги — физически: бросить пачку / упаковать в инвентарь), `/jobs`, `/jobcancel`, `/jobpost` (лидер: форма заказа/вакансии — зона там, где стоишь), `/trunk`, `/багажник` (открыть/закрыть багажник машины в прицеле; ЛКМ по слоту — стак, SHIFT+ЛКМ — 1 шт), `/ach`; E по терминалу «Биржа труда» — вакансии, заказы и вакансии фракций; F4 → «Работа»/«Ачивки»; ежедневный бонус — автоматически при входе

**Админ / Руководство:** `/grm_admin`, `/feco_admin` (полная экономика: фракции/ЗП/гос.бюджет/балансы), `!econadmin`, `/grm_admin` (ЕДИНАЯ панель: сервер/доступы/биржа/игроки/меню), `/factions`, `/salary_admin`, `/logistics_admin`, `/models_admin`,
`/weapons_admin`, `/mask_admin`, `!grmmenu`/`!grmadmin`/`!econadmin`, `/scanvehicles`,
`/spawnmenu`, `/vshop_admin`, `/phoneshop_admin`, `/phone_access`, `/door_access`, `/warrant`, `/unwarrant`, `/warrants`,
`/permadd`, `/permremove`, `/permlist`, `/permload`, `/radiomic_add`, `/radio_add`, `/speaker_add`, `/board_add`, `/board_allow <фракция>`, `/bcast_allow <фракция>`, `/alert_allow <фракция>`,
`/jobcenter_add`, `/jobcenter_remove`, `/jobdepot_add`, `/jobdepot_remove`, `/job_allow <фракция>`, `/job_deny <фракция>`, `/job_list`, `/ach_reset <ник>`, вкладка «Доступы» в `/factions` (доска/эфир/оповещение/**биржа**),
`/rack_add`, `/antenna_add`, `/rstation_add` (+`_remove`), `/rn_status` (радиосеть: стойки/антенны/передатчики/покрытие), мегафон — оружие `weapon_grm_megaphone` (Q → Оружие / `give`)

**Только владелец сервера (Root Guard, Код 84; сид-корневой SteamID зашит в коде):**
`/root_list`, `/root_queue`, `/root_add <STEAM_>`, `/root_del <STEAM_>`

Подробный разбор архитектуры и замеченных проблем — в `ANALYSIS.md`.
