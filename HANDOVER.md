# HANDOVER — памятка для следующей сессии агента (и для владельца)

Если ты — новая сессия Arena-агента в этом репозитории: **не работай от master** —
он почти пустой (снапшот пра-истории). Вся актуальная база в ветке:

```
git fetch origin arena/019f6cb8-drstrasse
git reset --hard FETCH_HEAD   # в своей рабочей ветке сессии
```

После этого прочитай `README.md` (реестр файлов и модулей) и `ANALYSIS.md`
(находки 1–101 — там вся история разработки и уроки, НЕ повторяй старые ошибки).

## Где что лежит
- `lua/` — весь код (основные модули ядра + `grm_single_addon (4)` + двери v2.0.3 + FFD Fading Door + FFD Keypad).
- Ключевые:
  - Код 42 `lua/autorun/sh_grm_currency.lua` (валюта **v2.0.2**),
  - Код 43 `lua/autorun/sh_grm_economy.lua` (экономика **v3.0.3**, банк, зеркало `grm_bank_nicks.json`),
  - Код 50 `lua/autorun/sh_grm_perm_entities.lua` (пермы энтити **v1.1.0** с поддержкой CCTV и АТС/сигнализаций),
  - Коды 51–54 (CCTV видеонаблюдение),
  - Коды 55–58 (Сигнализации),
  - Коды 59–60 (Система розыска Wanted),
  - Коды 61–62 (`sh_grm_doors.lua` v2.0.3 и `sh_grm_doors_access.lua` v2.1.0),
  - Коды 63–64 (RP Чат & EasyChat),
  - Код 65 (`grm_item_drop` и команда `/drop`),
  - Код 66 (`ds_key_swep` — дверные ключи),
  - Код 67 (`ds_battering_ram` — полицейский таран),
  - Код 68 (`ds_lockpick` — QTE-отмычка),
  - Код 69 (`ffd_fading_door` — исчезающие двери с нумпадом и таймером),
  - Код 70 (`grm_keypad` & `ffd_keypad` — кодовый замок с 3D2D дисплеем, PIN, фракционным и платным доступом).
- `dist/` — готовые zip для владельца (`grm_single_addon.zip`, `grm_economy.zip`, `grm_fix_hud_tab_currency.zip`, `grm_full_code.zip`).
  Владелец качает по raw-линкам на ветке — **ветку не удалять**.
- `tools/luatest/roundtrip_test.lua` — мок GMod API + 14 фаз юнит-теста персистентности. Обязателен к прогону после любой правки ядра.
- `tools/glua_check.py` — синтаксический чекер GLua.
- `tools/build_dist.py` — автосборка всех 4 архивов дистрибутива.

## Правила работы (стоящие приказы владельца)
- Язык общения — русский; владелец требователен — отвечать строго по делу.
- **Код не слать в чат** — только коммиты/архивы/ссылки + проза.
- После любой правки lua: `python3 tools/glua_check.py` + прогон `roundtrip_test.lua` + пересборка ВСЕХ 4 zip (`python3 tools/build_dist.py`) + обновить `README.md` + `ANALYSIS.md` + **commit+push сразу же**.
- Следующий свободный номер модуля: **Код 87**.

## Грабли среды (выстрадано)
- `/tmp` стирается между ходами; `/home/user` вне репо откатывается.
- LuaJIT для стенда собирается в `.luabuild`:
  `mkdir -p .luabuild && cd .luabuild && curl -fsSL -o lj.tar.gz https://codeload.github.com/LuaJIT/LuaJIT/tar.gz/refs/heads/v2.1 && tar xzf lj.tar.gz && mv LuaJIT-2.1 lj && cd lj && make -s`
  → бинарь `.luabuild/lj/src/luajit` (удалить `.luabuild` перед коммитом или держать в `.gitignore`).
- Запуск тестов: `rm -rf tools/luatest/data && ./.luabuild/lj/src/luajit tools/luatest/roundtrip_test.lua save` и далее все фазы.

## Уроки кода (главные)
- **Никогда не парсить JSON для ключей-SteamID64 голым `util.JSONToTable`** — третий аргумент обязателен `(txt, false, true)`; иначе ключи калечатся (находка 65).
- Там, где можно — хранить МАССИВАМИ записей, а не картами с числовыми ключами.
- Каждая запись на диск печатается с причиной (`SAVE ok ... [причина]`).
- В GMod `file.Write` возвращает nil: контроль записи — только read-back.

## Текущее состояние
- Работают: радиовещание+оповещение (Код 75), доска набора (Код 76), биржа труда v1.1.0 с эскроу-заказами и ВАКАНСИЯМИ фракций (Код 77, `/jobpost` — зона смены там, где стоит лидер; модель терминала — `models/lt_c/holo_wall_unit.mdl`, фолбэки консоль→радио, автомат с газировкой из цепочки убран), ачивки v1.0.1 с наградами и ежедневным бонусом (Код 78), единая админ-панель `/grm_admin` (Код 79), багажник любого транспорта `/trunk` (Код 80), деньги физически `/dropmoney`//`/money_pack` (Код 81, валюта 2.0.3), **диллер 2.1** (Код 82): окно с вкладками «Каталог» (поиск/3D-превью/баланс/двойной клик = купить) и «Мой транспорт» (замок/дистанция/возврат, удалённая сдача у дилера, «Убрать всё», автообновление 5 с по VD_MyListReq) + ЕДИНАЯ серверная точка удаления `_G.VD_RemoveDealerVehicle` (меню/C-меню/сторонние модули — одна логика, C-кнопка с суммой возврата и двойным подтверждением), Q-меню/инструменты игроков v1.1.0 (Код 83: конфиг синкается клиентам GRM_QMenu_Sync, закрытие Q глушит бинд +menu универсально — ванила И кастомные спавн-меню), **Защита владельца Root Guard v1.0.0 (Код 84): удаление фракций не-root суперадмином — только после одобрения владельца (сид SteamID зашит в sh_grm_rootguard.lua, окно подтверждений, /root_add|del|list|queue, fail-closed)**; доска v1.1.0 — автозачисление вступающих в заданный лидером отдел/должность; хаб v1.1.1 (+«Экономика»: гос.бюджет/бюджеты/наличные); экономика 3.0.4 (экспорты StateBudget*/FactionInfo); F4 v1.4.6 расширяем вкладками модулей (хук `GRM_F4_BuildTabs`: «Работа», «Ачивки»).
- **Радиосеть RadioNet v1.0.0 (Код 85, sh_grm_radionet.lua):** инфраструктура вещания — серверная стойка (`grm_server_rack`, E=питание, репитер 1200 юн), антенна (`grm_antenna`, активна у включённой стойки, круг покрытия 3200 — усиление сигнала), радиопередатчик (`grm_radio_station`, стык микрофона с сетью); эфир до приёмников только в покрытии + «радио-искажение» (детерминированные выпадения ∝ качеству + треск/щелчки — усиление=маршрут is3D=false, чистый Lua поток не деформирует); режим микрофона ГРОМКАЯ СВЯЗЬ (доступ /alert_allow, голос усиленно из сетевых громкоговорителей); гейт раций по покрытию (вне сети — 1500 напрямую); /alert только по сетевым громкоговорителям; мегафон `weapon_grm_megaphone` (1600 юн рупор); единая маршрутизация `GRM.RadioNet.VoiceRoute` (телефония консультирует первой — убрана гонка pairs() хуков голоса, находка 100); хуки BC делегируют ей (v1.2.0); автоперсист `grm_rnents/<map>.json`; спавн `/rack_add` `/antenna_add` `/rstation_add`, диагностика `/rn_status`; стенд sim_radionet (58 проверок).
- **Медицинские карты + заказ 5-в-1 (Код 86, sh_grm_medical.lua v1.0.0):** врачи ведут карты пациентов (диагнозы актив/излечён, записи/назначения/операции, показания кровь+аллергии+хроника, кап 60 записей), доступ через Factions по фракциям/рангам/отделам (fail-closed, UI «Доступ» суперадмину, s64-фолбэк н101); /medcards /mycard grm_medcards. **Штрафы починены в корне (находка 101):** экономика искала члена фракции ТОЛЬКО по SteamID-ключу Members — memberRec (sid ИЛИ s64) во factionOf/onlineMembers/CanFine; /fine ником или прицелом (до 250), новая /fines — права+лимит+последние 5 штрафов фракции (настройка в /feco_admin → save_entry → fine, дефолт ВЫКЛ). **Логистика:** simfphys-машины вне дилера распознаются (ent.VehicleName-ключ, sh_vehicle_keys.lua:103), /logistics_start + grm_logistics_start называют точную причину отказа, grm_logistics_debug суперадмину, пустые точки — окно-подсказка. **Q-меню v2.0.0:** при playersQ=false бинд +menu открывает «GRM Стройку» (каталог пропов propList с лимитом и rate-limit, выдача toolgun с CanUseTool-фильтром, уборка своих пропов) — абуз ванили исключён архитектурно; /qm_prop_add[model]/_del/_list/_clearprops суперадмину; флаги grmBuildMenu/propsFree в хабе «Инструменты». **C-меню:** кнопки «Выбросить деньги…» и «Передать деньги: Имя» (aim ≤300, протокол GRM_Ctx_MoneyAct, антифлуд 0.8с). F4 v1.4.7; стенд 14 фаз (+fines), sim_medical 55 проверок.
- Factions Extended v3.1.1: вкладка «Расширенные настройки» /factions на хуке `GRM_FactionsAdmin_BuildTabs` (ком.час статус/отмена, живые счётчики моделей-оружия-маскировки, gnews; авто-синк 1.5 с); buildSyncData зеркалит модели/оружие/gnews; оружие сохраняется в fw_faction_extras.json. Подсказки управления у машин при прицеле вблизи.
- Аудит протоколов net-кнопок: `tools/proto_audit.py` (прогон чист, в регресс наряду с glua_check; whitelist внешних/циклических протоколов внутри файла — при новых модулях сверять).
- Изолированные сим-стенды: `tools/luatest/sim_broadcast.lua`, `tools/luatest/sim_jobs.lua`, `tools/luatest/sim_trunk.lua`, `tools/luatest/sim_money.lua`, `tools/luatest/sim_qmenu.lua`, `tools/luatest/sim_dealer.lua` (реальный dealer init.lua грузится трансформацией `continue`→goto; стабы ConVar/include/ENT-метатаблица), `tools/luatest/sim_rootboard.lua` (Root Guard + доска-assign + qmenu КЛИЕНТ-пассом: файл грузится дважды со сменой SERVER/CLIENT и LocalPlayer-стабом; vgui-стаб ANY-объект для build-меню v2.0.0), `tools/luatest/sim_radionet.lua` (топология сети/покрытие/маршруты голоса/выпадения/FX/персист, 58 проверок; стенду нужны `__mul`/`Angle`/`table.Count` у стабов), `tools/luatest/sim_medical.lua` (медкарты Код 86: права ролей/отделов/s64-ключей, журнал/кап, своя/чужая карта, персист — 55 проверок) — паттерн доказательства чат-контракта и жизненных циклов без GMod.
- Тесты пройдены 14/14 (100% OK), синтаксис всех 238 Lua-файлов ядра 100% валиден, 9 сим-стендов зелёные, proto_audit чист.
